// 加载用户信息
const loadUserInfo = () => {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      // 获取用户信息，并保存到vuex里面（如果失败请用ui组件进行提示） TODO
      resolve()
    }, 500)
  })
}

export default loadUserInfo