// 加载菜单列表
const loadMenus = () => {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      // 获取菜单，注册动态路由（如果失败请用ui组件进行提示） TODO
      resolve()
    }, 1500)
  })
}

export default loadMenus;